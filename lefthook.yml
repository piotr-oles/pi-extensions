commit-msg:
  commands:
    commitlint:
      run: npm exec -- commitlint --edit {1}

pre-commit:
  parallel: true
  commands:
    check:
      run: npm run check
    typecheck:
      run: npm run typecheck
