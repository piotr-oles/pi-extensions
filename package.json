{
  "name": "pi-extensions",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.30.1",
  "engines": {
    "node": ">=22.19.0"
  },
  "type": "module",
  "scripts": {
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "check": "pnpm -r run check",
    "fix": "pnpm -r run fix",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm install --frozen-lockfile && changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@changesets/cli": "^2.31.0"
  }
}
