Think in code:
Python: Use `python3` to process data instead of reasoning by hand.
TypeScript: Use `npx tsx <<'EOF' ... EOF` (run from the package dir that owns the deps)
When processing data, store results in a temp file, use bash & grep to retrieve.
