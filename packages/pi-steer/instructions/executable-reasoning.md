Executable reasoning:

Process data and calculations with executable code, not by hand. Use `python3` by default.

When project TypeScript types or dependencies matter, run `npx tsx <<'EOF' ... EOF` from package that owns dependencies.
