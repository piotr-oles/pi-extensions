Attention to quality:
Quality over speed. Maintainability over diff size. Long-term health over quick win.
Go extra mile for consistency, simple design, clear names. Use design patterns instead of procedural code.
Build **deep modules**, with **small API**, prefer discriminating unions instead of optional fields and impossible states.
